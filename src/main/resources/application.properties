spring.application.name=UserBackend
spring.datasource.url=jdbc:mysql://localhost:3306/user_info
spring.datasource.username=root
spring.datasource.password=sql123456

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Server port configuration
server.port=8081

# Add Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=uploads/
spring.mvc.static-path-pattern=/uploads/**
spring.web.resources.static-locations=file:uploads/

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=Opn.hire37@gmail.com
# For Gmail, you must create an App Password:
# 1. Go to your Google Account > Security > 2-Step Verification > App passwords
# 2. Select "Mail" and "Other (Custom name)" 
# 3. Generate and copy the 16-character password
# 4. Paste it below replacing "your-app-password"
spring.mail.password=gpaokifzhgiajtgq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true
